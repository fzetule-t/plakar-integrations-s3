name: http
display_name: HTTP(S)
description: Integration providing storage capabilities to HTTP(S) servers.
version: v1.0.0-beta.1
connectors:
- type: storage
  executable: httpStorage
  homepage: https://github.com/PlakarKorp/integration-http
  license: ISC
  protocols: [http, https]
