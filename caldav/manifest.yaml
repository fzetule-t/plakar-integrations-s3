name: caldav
description: caldav backend
version: 1.0.0
connectors:
  - type: importer
    executable: importer-caldav
    protocols: [caldav]
  - type: exporter
    executable: exporter-caldav
    protocols: [caldav]
